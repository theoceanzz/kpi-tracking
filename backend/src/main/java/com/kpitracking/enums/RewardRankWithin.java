package com.kpitracking.enums;

/** Phạm vi so kè khi xếp hạng. */
public enum RewardRankWithin {
    /** Một bảng xếp hạng chung cho toàn bộ phạm vi của chương trình. */
    SCOPE,
    /** Xếp hạng riêng trong từng đơn vị — dùng khi muốn "top của mỗi phòng". */
    PER_UNIT
}
